# dynamixel_driver parameters
dynamixel_driver_node:
  ros__parameters:
    # U2D2 serial port (check with: ls /dev/ttyUSB*)
    port_name: "/dev/ttyUSB0"
    baudrate: 57600

    # Motor IDs — your 4 Dynamixel XH540-W140-R motors
    motor_ids: [1, 6, 8, 10]

    # Motor layout (looking from behind the robot):
    #   Front-Left: ID 1    Front-Right: ID 6
    #   Rear-Left:  ID 8    Rear-Right:  ID 10
    #
    # Right-side motors spin in reverse direction for forward motion
    reverse_ids: [6, 10]

    # Operating mode: "velocity" or "position"
    operating_mode: "velocity"

    # Velocity limits (Dynamixel unit: ~0.229 RPM per unit)
    # XH540-W140 max is ~37 RPM → ~161 units
    max_velocity: 100       # conservative limit
    deadzone: 0.15          # joystick deadzone (0.0 - 1.0)

    # Control loop rate (Hz)
    loop_rate: 50.0

    # Safety: auto-stop if no command received for this many seconds
    cmd_timeout: 0.5
